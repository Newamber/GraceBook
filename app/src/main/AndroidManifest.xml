<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.newamber.gracebook">

    <application
        android:name=".GraceBookApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="true"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".view.activity.AddAccountActivity">
        </activity>

        <activity android:name=".view.activity.SettingsActivity">
        </activity>

        <activity android:name=".view.activity.TypeEditActivity">
        </activity>

        <activity android:name=".view.activity.SplashActivity">
        </activity>

    </application>

</manifest>